This parameter is used solely for versioning the data structure of SpineOpt.
Effectively, it keeps track of what migration scripts need to be run to
transition to later versions.

Users should never have the need to change it manually, and never should.