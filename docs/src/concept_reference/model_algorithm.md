Select the algorithm to be run from the [model\_algorithm\_list](@ref).

See the *Algorithms* chapter for descriptions of (some of) the supported ones.
E.g. [Decomposition](@ref),
[Modelling to generate alternatives](@ref mga-advanced),
and [Multi-stage optimisation](@ref).