Adds a term to the right-hand side of [unit flow ratio constraint](@ref constraint_ratio_unit_flow),
multiplying the [units\_started\_up](@ref var_units_started_up) variable
with the [flow\_ratio\_start\_flow](@ref).