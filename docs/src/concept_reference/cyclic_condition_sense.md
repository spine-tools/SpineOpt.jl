Sets the sense for the [cyclic node state constraint](@ref constraint_cyclic_node_state).
The default value of `>=` forces the [node\_state](@ref var_node_state) variable to end up
at a value higher than where it started from.