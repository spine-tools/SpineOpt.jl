In the MGA algorithm the original problem is re-optimized (see also [mga-advanced](@ref)), and finds near-optimal solutions. The parameter [mga\_max\_slack](@ref) defines how far from the optimum the new solutions can maximally be (e.g. a value of 0.05 would allow for a 5% increase of the original objective value).
